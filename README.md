# Android_File_Segregator
Python Code to move files to directories based on created date(in file name)

Usage

pip install -r requirements.txt


modify the 'config' in action.py

python action.py or python3 action.py